"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CodeGeneratorConfig = void 0;
const path_1 = __importDefault(require("path"));
class CodeGeneratorConfig {
    constructor(config) {
        this.config = config;
    }
    static set(config) {
        const resolvedConfig = Object.assign(CodeGeneratorConfig.INSTANCE.config, config);
        CodeGeneratorConfig.INSTANCE = new CodeGeneratorConfig(resolvedConfig);
    }
    static get() {
        return CodeGeneratorConfig.INSTANCE.config;
    }
}
exports.CodeGeneratorConfig = CodeGeneratorConfig;
CodeGeneratorConfig.INSTANCE = new CodeGeneratorConfig({
    srcPath: `${process.cwd()}${path_1.default.sep}src`,
    language: {
        name: 'typescript',
        fileExtension: 'ts'
    },
    runtime: 'node',
});
